# 2026-09-09

## 全站内容体检（只读检查，未改任何站点文件）

对 cloud1990.github.io（Jekyll 课题组主页，CNAME: www.xuyuncheng.com，GitHub Pages 部署）做全量内容审查。

关键结论：
- 线上 = GitHub Pages 服务，Last-Modified 2026-09-01，与 master 最新提交 f2e4b38 同步，工作区干净。
- 全部照片/视频/GIF 引用完整；10 页面 + 8 数据文件 + 6 导航均正常。

发现的问题（按严重度）：
1. **图标失效（高）**：`_includes/head.html` 第 11–18 行整块注释把 Font Awesome 本地 css（行17 `/assets/font-awesome.min.css`）也注释掉了，线上只加载 main.css + academicons。页面所有 `fa fa-*` 图标（邮件/主页/GitHub/PDF 下载等）不显示。assets/font-awesome.min.css 存在且可访问，取消注释行 17 即可修复（CDN 行可保持注释）。
2. **安全（高）**：`.git/config` origin URL 明文内嵌 GitHub 令牌（ghp_ 开头），建议撤销并改用 SSH/凭据管理器。
3. **课程 PDF 缺失 3 个**（courses.md 注释块引用，当前不可见，取消注释即断链）：files/Academic_English/ 下缺 Pump.pdf、Technical_Terms.pdf、专业英语教学资料（部分考题出处）.pdf。
4. **遗留数据**：collaborators.yml（9 位美国教授，模板遗留）在 /about/ 显示；funders.yml（NSF/DOE/ONR/NIH）无引用；about.md 仍引用已删除的 awards 与不存在的 people（区块静默隐藏）；about 页不在导航中。
5. 出版物年份断层：缺 2018/2019/2023/2024（需确认是否本无成果）。
6. news.yml 2025 年条目排序错乱（1 June 2025 在 15 May 2025 前）；成员"7月入学"与新闻"9月入学"口径并存。
7. teampic 有未引用素材：qiao2025.gif、qiao2025_1.gif、yu2024.gif（共~9.3MB）、yuncheng_xu_1.jpg、yuncheng_xu_old.jpg；20260617.jpg 7.5MB 过大。
8. 小瑕疵：bibtemplate.html `height:20px:` 缺分号；default.html 统计脚本在 `<head>` 前；baidu-site-verification meta 重复；.htaccess 列入 include 但不存在。

备注：本地 _site 为 2026-07-08 旧构建且已 gitignore，与线上部署无关（GH Pages 源码构建）。

## 追加：Git 凭据泄露修复（已完成）
- 排查：令牌仅存在于 .git/config origin URL 一处，仓库其他文件无散落。
- 操作：`git remote set-url origin https://github.com/cloud1990/cloud1990.github.io.git`，已清除明文令牌。
- 系统凭据助手为 helper-selector（Git Credential Manager），后续 push 自动走 GCM，无需恢复令牌。
- 待用户操作：到 GitHub → Settings → Developer settings → Personal access tokens 撤销原令牌（已泄露即须作废）。若撤销后 push 报 401，执行 `git credential-manager github logout` 后重新 push 走浏览器登录。

## 追加：全站图标失效修复（已完成）
- 因由：`_includes/head.html` 第 11–18 行整块 HTML 注释把本地 `/assets/font-awesome.min.css` 也包进去了。
- 修复：收窄注释块，仅保留 CDN/favicon 注释，让本地 FA 与 academicons 两行样式独立加载。
- 关键点：font-awesome.min.css 的 @font-face 回退列表含 `fontawesome-webfont.ttf`，而仓库 fonts/fontawesome-webfont.ttf 存在，现代浏览器走 ttf 回退，图标正常。
- 验证：`ruby -S jekyll build`（Windows 下用 C:\Ruby33-x64\bin\ruby.exe，直接 -S 调用，不用 bundle，因 bundle.bat 有路径转义坑）构建成功；产物 _site/fonts/fontawesome-webfont.ttf、_site/assets/font-awesome.min.css 均输出，首页 HTML 已引入该 css。
